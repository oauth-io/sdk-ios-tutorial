#include "Pods-OAuth.io.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_OAUTH_IO_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/OAuth.io" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/OAuth.io"
OTHER_LDFLAGS = -ObjC ${PODS_OAUTH_IO_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}